<UserControl x:Class="HMI_Tool.Switch.Switch"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HMI_Tool.Switch"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="80">
    <Grid>
        <!-- Background -->
        <Border x:Name="SwitchBackground"
                CornerRadius="20"/>

        <!-- Switch Circle -->
        <Ellipse x:Name="SwitchCircle"
                 Width="34" Height="34"
                 Fill="White"
                 HorizontalAlignment="Left"
                 Margin="3"
                 RenderTransformOrigin="0.5,0.5">
            <Ellipse.RenderTransform>
                <TranslateTransform x:Name="SwitchTranslateTransform"/>
            </Ellipse.RenderTransform>
        </Ellipse>

        <!-- Click Area -->
        <Button Background="Transparent" BorderThickness="0" Click="Switch_Click"/>
    </Grid>
</UserControl>
