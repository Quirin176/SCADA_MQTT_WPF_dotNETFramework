<UserControl x:Class="HMI_Report.Report_View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HMI_Report"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1500">
    <Grid Background="White" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Report Selection -->
        <StackPanel Orientation="Horizontal">
            <TextBlock FontSize="16" FontWeight="Bold" Text="Select Report:" VerticalAlignment="Center" Margin="300,0,10,0"/>
            <ComboBox x:Name="comboBoxReports" FontSize="16" Width="200" SelectionChanged="comboBoxReports_SelectionChanged" Margin="10,0,100,0"/>

            <TextBlock FontSize="16" FontWeight="Bold" Text="Start:" VerticalAlignment="Center" Margin="10,0,10,0"/>
            <DatePicker x:Name="pick_StartDate" FontSize="16" Margin="10,0,100,0"/>

            <TextBlock FontSize="16" FontWeight="Bold" Text="End:" VerticalAlignment="Center" Margin="10,0,10,0"/>
            <DatePicker x:Name="pick_EndDate" FontSize="16" Margin="10,0,10,0"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,10,0,10">
            <Button FontSize="16" FontWeight="Bold" Content="View" Width="100" Background="White" BorderBrush="Gray" BorderThickness="2" Click="btn_View_Click" Margin="300,0,10,0"/>
            <Button FontSize="16" FontWeight="Bold" Content="Export" Width="100" Background="White" BorderBrush="Gray" BorderThickness="2" Click="btn_Export_Click" Margin="300,0,10,0"/>
            <Button FontSize="16" FontWeight="Bold" Content="Clear" Width="100" Background="White" BorderBrush="Gray" BorderThickness="2" Click="btn_Exit_Click" Margin="300,0,10,0"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="dataGrid" Grid.Row="2" AutoGenerateColumns="True" IsReadOnly="True"
                  Margin="0,0,0,10"/>

        <!-- Status -->
        <TextBlock Grid.Row="3" x:Name="txtStatus" Foreground="Gray"/>
    </Grid>
</UserControl>
